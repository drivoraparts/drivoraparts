/* =========================================================
   AFTERMARKET INVENTORY
   ---------------------------------------------------------
   The previous entries were sample/test data only and have
   been cleared. The `aftermarket` category itself is kept
   (see categories.ts) so real products can be imported here.

   Add real aftermarket products to the array below using the
   same `Product` shape as lib/inventory/products.ts.
========================================================= */

import type { Product } from "./types";

export const aftermarketProducts: Product[] = [];
