import FooterContent from "./FooterContent";

export default function GlobalFooter() {
  return (
    <footer className="mt-20 w-full min-w-0 max-w-full overflow-x-clip border-t border-white/10 bg-black text-white">
      <FooterContent />
    </footer>
  );
}
