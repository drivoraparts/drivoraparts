"use client";

import TrustBadgeStrip from "./TrustBadgeStrip";

/** Pro product sheet trust row — reuses marketplace badge content in white layout. */
export default function ProTrustBadges() {
  return <TrustBadgeStrip variant="pro" />;
}
